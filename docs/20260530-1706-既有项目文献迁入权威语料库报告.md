# 既有项目文献迁入权威语料库报告

生成时间：2026-05-30 17:06 CST

## 结论

原项目 `data/raw_pdfs` 下的 55 篇 PDF 已经迁入 `/root/datasets/chinese_landscape_authority_corpus` 的统一语料体系。

处理方式不是直接混入权威种子资料，而是单独放入 `07_既有项目文献`，并标注为 `legacy_project_pdf`。这批资料可以进入 RAG，但权重低于古代画论原典、博物馆图录、故宫专题论文和馆藏条目。

## 迁入结果

- 原始项目 PDF：55 个。
- 完整性检查：55 个均通过 `%PDF-` 与 `%%EOF` 检查。
- 独立复制迁入：54 个。
- 完全重复跳过：1 个。
- 迁入失败：0 个。

重复文件：

- `data/raw_pdfs/从敦煌壁画看唐代青绿山水.pdf`
- 与已有权威来源 `dpm_dunhuang_tang_qinglv_2018` 的 SHA256 完全一致。
- 已写入来源记录并标注 `duplicate_skipped`，但不重复进入导入队列。

## 当前语料规模

来源记录：

- `/root/datasets/chinese_landscape_authority_corpus/metadata/sources.jsonl`：97 条。

可导入文档：

- `/root/Workspace/ShanshuiAgent/data/metadata/文献级标注清单.jsonl`：94 条。
- `/root/Workspace/ShanshuiAgent/data/metadata/高权重导入队列.jsonl`：94 条。
- `/root/Workspace/ShanshuiAgent/data/metadata/文献多维标注清单.jsonl`：94 条。

按类别：

- `01_古代画论原典`：7 个。
- `02_博物馆图录专题`：8 个。
- `03_作品教学资料`：2 个。
- `04_故宫专题论文`：9 个。
- `05_发展脉络与风格流派`：10 个。
- `06_作品级馆藏条目`：4 个。
- `07_既有项目文献`：54 个。

按权威等级：

- `A`：9 个。
- `A-`：31 个。
- `B`：49 个。
- `C`：5 个。

其中原 55 篇里，有明确题名的文献默认标为 `B`；`paper1.pdf` 这类题名不明文件默认标为 `C`。

## 文件位置

迁入后的 PDF：

- `/root/datasets/chinese_landscape_authority_corpus/raw_pdfs/07_既有项目文献/`

新增脚本：

- `/root/Workspace/ShanshuiAgent/scripts/datasets/import_existing_project_pdfs.py`

更新后的索引：

- `/root/Workspace/ShanshuiAgent/data/metadata/文献级标注清单.jsonl`
- `/root/Workspace/ShanshuiAgent/data/metadata/高权重导入队列.jsonl`
- `/root/Workspace/ShanshuiAgent/data/metadata/文献多维标注清单.jsonl`
- `/root/Workspace/ShanshuiAgent/data/metadata/文献多维分类索引.json`
- `/root/Workspace/ShanshuiAgent/data/metadata/文献多维分类索引.md`

## 多维分类更新

迁入后已经重建多维分类索引。当前按发展阶段统计：

- `明代：吴门、松江与南北宗`：26 篇。
- `清代：正统派、四王四僧与笔墨再阐释`：26 篇。
- `宋代：山水成熟与理论化`：25 篇。
- `元代：文人画与复古传统`：19 篇。
- `隋唐五代：早期山水与青绿传统`：18 篇。
- `跨朝代/通史`：11 篇。
- `近现代：中国画转型`：3 篇。

旧文献也会进入 `lineages_schools`、`styles_techniques`、`themes` 等维度。例如：

- `明代北宗山水画风格研究`：明代、南北宗论述系统、风格流派。
- `山水画留白艺术探究`：留白与虚实、风格技法。
- `镣铐下的舞蹈——清代初期_四王_山水画现象透视`：清代、清初四王与正统派、风格流派。

## 注意事项

这批旧文献的主要问题不是文件完整性，而是来源元数据不足：多数缺少来源 URL、出版信息、授权说明和权威层级判断。因此后续应当：

- 保留它们用于召回补充。
- 不让它们在检索权重上高于原典、博物馆图录、故宫专题论文。
- 后续逐篇补 `author`、`source_url`、`publication`、`year`、`institution`。
- 对 `paper1.pdf` 到 `paper5.pdf` 这类题名不明文件优先做内容识别和重命名。

## 对 RAG 的含义

现在可以把 94 个可导入文档统一进入新 RAG 证据链路。导入时每个 chunk 应继承文献级字段：

- `doc_id`
- `authority_level`
- `rag_import_weight`
- `source_roles`
- `periods`
- `dynasties`
- `lineages_schools`
- `styles_techniques`
- `persons`
- `works`
- `themes`

这样既能利用旧文献的覆盖面，又不会让来源不明的材料压过权威证据。
